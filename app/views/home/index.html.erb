<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<div data-controller="hello">
</div>
<% if current_user %>
  
  <%= image_tag(current_user.picture_url) %>
  
  User: <%= current_user.name %> of <%= current_user.email %>
  
  <br>
  
  <%= link_to "Edit profile", edit_user_registration_path %>
  
  <br>

  <%= button_to 'Log out', destroy_user_session_path, data: {turbo: "false"}, method: :delete %>

  <br>

  <%= %>
  <%= button_to 'Create Event', create_event_path, data: {turbo: "false"}, method: :post %>
  
<% else %>
  <%= link_to 'Log in', new_user_session_path %>
  <br>
  <%= link_to 'Create Account', new_user_registration_path %>
<% end %>